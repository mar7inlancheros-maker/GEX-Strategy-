# Dependencias verificadas contra los imports reales del codigo (2026-09-04).
# Versiones exactas del entorno donde se produjeron los resultados de docs/RESULTADOS.md.

databento==0.64.0        # cliente oficial: descarga OPRA y equities
polars==1.36.1           # dataframes; todo el pipeline de datos
pyarrow==21.0.0          # backend parquet de polars
numpy==2.0.2
pandas==2.3.3            # solo en la frontera con databento y yfinance
scipy==1.13.1            # spearmanr en la puerta P2b
statsmodels==0.14.6      # OLS con errores Newey-West (HAC)
numba==0.60.0            # arbol CRR vectorizado
yfinance==1.2.0          # backfill de equities 2021-2024 (ver docs/RESULTADOS.md)
